import React from 'react';

const Header = () => (
    <header>
        <nav>
            <ul>
                <li>Header</li>
            </ul>
        </nav>
    </header>
);

export default Header;